<!--
本文件是 PRD 骨架，不是最终输出。写作提示与示例全部写在 HTML 注释里，输出时必须删除。

用法：
1. 每个页面从下面三种骨架里选一种（列表型 / 详情型 / 表单型）套用，判断依据见 SKILL.md「四、执行步骤」第 4 步。
2. 章节名可按页面实际情况微调，但保持「一、二、三」的顺序与层级。
3. 标「条件章节」的，无内容时整节删除，不写「无」、不留空标题。
4. 所有 <...> 占位符与 YYYY-MM-DD 必须替换为实际内容。

层级 / 字重 / 版式 / 归属 / 禁止等写作规则见 SKILL.md「五、写作约定」，本文件不重复定义。
-->

# <需求名称> PRD

> 版本 v1.0　·　日期 YYYY-MM-DD

*需求场景：<一句话讲清谁、在什么场景下、遇到什么问题、因此需要做什么。>*

<!-- 必填。一整句话，不拆成多句、不分行。对应原型标注右上角的「需求场景」框。
不展开功能方案，不描述技术实现。

示例：
*需求场景：平台、景区商家与渠道方需要按账期核对每笔订单的分成比例与分账结果，
并对分账失败的订单提供失败原因查看与重试入口。* -->

## 变更记录

<!-- 条件章节：首版写一行「初版」即可；无迭代需求时整节删除。
按版本倒序，最新在上。本节记「这一版改了什么」；
正文里的「X月X日补充：……」记「这一版内追加的细节」，两者不互相重复。 -->

| 版本 | 日期 | 变更内容 | 提出人 |
| --- | --- | --- | --- |
| v1.0 | YYYY-MM-DD | 初版 | <姓名> |

## <页面一名称>

<!-- ▼ 列表型骨架。页面标题用原型 / 菜单里的实际名称。
· 列表型 —— 主体是筛选条件 + 数据表格 + 行内操作（发放记录、订单管理）
· 详情型 —— 主体是汇总数据 + 明细列表（对公结算详情、订单详情）
· 表单型 —— 主体是录入 / 配置字段（新增租户、编辑配置） -->

### 一、筛选

<!-- 条件章节：页面无筛选条件时整节删除。
逐条说明每个筛选项的控件类型、默认值、选项范围、匹配方式。
只有一句话说明、无并列项时不必编号；多条并列时必须编号。 -->

1. **<字段名>**：<控件类型>，默认「<默认值>」，支持<匹配方式>
2. **<字段名>**：下拉单选，默认全部，包括「<选项1>」「<选项2>」「<选项3>」

<!-- 示例：
1. 支持用户手机号模糊查询
2. **状态筛选**：下拉单选，默认全部，包括全部状态、待使用、已核销、已作废、已过期 -->

### 二、列表说明

<!-- 必填。说明列表展示哪些列、每列口径，以及状态类字段的含义。
有「若…则…否则…」的展示分支时，拆成子项逐条写，不要挤在一句话里。
并列项达到 3 条以上（状态定义、取值枚举、校验规则、汇总口径）时用表格。 -->

1. **<列名>**：<数据来源 / 展示口径>
2. **状态说明**：

    | 状态 | 含义 | 进入条件 |
    | --- | --- | --- |
    | <状态值> | <含义> | <进入条件> |

3. **<列名>**
    - 若<条件>，展示<内容>
    - 若<其他情况>，展示「--」

<!-- 示例：
1. **核销订单号**
    - 若状态为「已核销」，展示关联的订单号
    - 若为其他，展示「--」
2. **创建时间**：阿里云同步
3. **更新时间**：上一次保存配置的时间 -->

### 三、操作说明

<!-- 条件章节：列表无行内操作时整节删除。
逐个操作说明：入口、可用范围（哪些行 / 哪些角色可操作）、点击后的结果、是否需要二次确认。
置灰、隐藏、二次确认文案都要写清楚，不要写「按权限控制」这类模糊表述。 -->

1. **<操作名>**：<可用范围>，点击后<结果>
2. **<操作名>**：需二次确认，弹窗文案「<确认文案>」

<!-- 示例：
1. **作废范围**：仅「待使用」状态的券支持作废。「已核销」或「已作废」或「已过期」的券，操作按钮置灰
2. **作废操作**：需二次确认，弹窗文案「确定作废该优惠券吗？」
3. **素材采集 —— 执行航线**：打开「下发素材采集任务」弹窗
    - 若未关联航线 / 航线被删除（即列表显示「--」），提示「未关联素材航线，请在「航线管理」页面配置」
    - 若已关联航线，弹窗选择执行航线和执行设备 -->

### 四、其他说明

<!-- 条件章节：无内容时整节删除。只收三类内容：
① 范围外说明 —— 本节不涉及、沿用原逻辑、暂不支持的部分
② 跨节整体行为 —— 排序、分页、空态、权限、数据来源
③ 版本内补充 —— 直接成行写「X月X日补充：……」，不另起章节
不属于这三类的，放回上面的对应几节，不要往这里堆。 -->

- **默认排序**：<排序字段 + 方向>
- **分页**：默认 <N> 条/页
- <X月X日补充>：<补充内容>

## <页面二名称>

<!-- ▼ 详情型骨架 -->

### 一、汇总说明

<!-- 必填。详情页头部汇总区：展示哪些汇总项，以及每项的计算口径。
有范围切换时，先说明切换行为，再逐条列出各汇总项口径。
汇总项必须写口径 ——「订单笔数」不算口径，「当前账期内支付成功的订单数量」才是。
汇总项达到 3 条以上时改用表格：| 汇总项 | 统计口径 | -->

1. **<字段1>与<字段2>**：展示<字段1> + <字段2>
2. **<范围控件>**：默认「<默认值>」，支持<切换行为>，切换后<刷新范围>
    - **<汇总项>**：<统计口径>
    - **<汇总项>**：<统计口径>

<!-- 示例：
1. **账期与账号**：展示账期 + 对应账号（名称）
2. **景区范围**：默认「全部」，支持切换与该景区商家 / 渠道相关的景区查看，切换后汇总数据和明细同步刷新
    - **订单笔数**：纳入当前账期、当前结算对象、当前景区范围的订单数量
    - **订单金额合计**：当前结算对象、当前景区范围内支付成功金额合计
    - **退款金额合计**：当前结算对象、当前景区范围内退款成功金额合计
    - **应结算金额**：当前结算对象、当前景区范围内应结算金额合计 -->

### 二、明细列表

<!-- 必填。说明明细区展示哪些字段、每列口径，以及字段上的交互。
凡带「当前 X 的 Y」这类相对口径的字段（分成比例、应结算金额），必须把口径写出来。
保留字段可以合并成一条写，不必逐个列。
行内操作写在本节，不写进「三、页面操作」；
本节的排序、分页、空态、页面数据来源属于跨节整体行为，写进「四、其他说明」。 -->

1. **保留字段**：<字段1>、<字段2>、<字段3>
    - 增加<字段>查看交互（<可操作角色>）：点击<内容>跳转<目标>，并支持返回
2. **<字段名>**：<计算口径>
3. **<字段名>**：<计算口径>

<!-- 示例：
1. **保留字段**：原订单号、订单主题、订单状态、下单时间、完成时间
    - 增加订单号查看交互（仅租户和平台可查看）：支持点击订单查看该订单详情，并支持返回
2. **分成比例**：当前结算对象在该订单的分成比例
3. **应结算金额**：当前结算对象在该订单的应结算金额 -->

### 三、页面操作

<!-- 条件章节：详情页无操作时整节删除。
指页面级操作（返回、导出、跳转），不写明细行内的操作 —— 行内操作写在「二、明细列表」里。
写法同列表型「三、操作说明」。 -->

### 四、其他说明

<!-- 条件章节。内容范围同列表型「四、其他说明」；不同之处：明细列表的排序、分页、空态、
页面数据来源都落在这里，不写进「二、明细列表」。 -->

- **数据来源**：<从哪取，何时刷新>
- **分页**：默认 <N> 条/页
- **空态**：<无数据时展示什么>

## <页面三名称>

<!-- ▼ 表单型骨架。分组章节的数量按页面实际分组增减，不固定为两节。 -->

### 一、<表单分组一>

<!-- 按页面上的分组标题拆节，一节 = 一个分组，顺序与页面从上到下一致。
每个字段一条，格式：字段名：必选性。默认值。取值 / 规则。
单字段的取值校验写在字段条目里（拆成子项），不要留到「三、提交与校验」再写。
凡「自动带出 / 自动拉取 / 自动计算」的字段，必须写明三件事：
① 数据来源（从哪取）② 刷新时机（何时重取）③ 取不到时的表现（展示什么、能否提交）
字段达到 5 个以上时改用表格：| 字段 | 组件 | 必填 | 默认值 | 规则 | -->

1. **<字段名>**：必选。默认「<默认值>」，备选项包含「<选项1>」「<选项2>」。
2. **<字段名>**：非必填。最多 <N> 字。
3. **<字段名>**：支持<自动带出 / 联动行为>，展示格式「<展示格式>」。
    - **数据来源**：<从哪取>
    - **刷新时机**：<何时重取>
    - **取不到时**：<展示什么、能否提交>

<!-- 示例：
1. **支付平台**：必选。默认选中「汇付天下」，备选项包含汇付、微信支付等。
2. **支付方式**：必选。默认选中「微信支付」。
3. **分账能力**：根据选中的商户号自动查询，展示状态为「已开通」或「未开通」。
    - 未开通分账时，租户下新增景区商家 / 渠道的分账方式禁选「线上自动分账」 -->

### 二、<表单分组二>

### 三、提交与校验

<!-- 条件章节：无提交动作（纯配置展示页）时整节删除。
只写跨字段、跨行、跨成员的校验，以及提交中 / 成功 / 失败的表现。
单字段的取值校验跟着字段走，本节不重复写一遍。
校验达到 3 条以上时改用表格：| 触发条件 | 提示文案 | -->

1. **提交校验**：<校验规则>，不通过时提示「<提示文案>」
2. **提交成功**：<关闭弹窗 / 返回列表 / 停留并刷新>，并提示「<提示文案>」

### 四、其他说明

<!-- 条件章节。内容范围同列表型「四、其他说明」；不属于这三类的，放回对应分组。 -->

- **编辑**：<是否允许编辑，不允许时说明原因>
- **<角色>**：<可查看 / 可操作范围>
- <X月X日补充>：<补充内容>

<!-- 示例：
- **编辑**：暂不允许编辑支付平台配置
- 8 月 11 日补充：创建租户控制选择支付平台的汇付账号**不能与其他租户重复**，
  已被选择的商户号在列表可见但置灰不可选 -->

## 待确认事项

<!-- 条件章节：无待确认事项时整章删除，不写「无」。
未确认的信息一律写在这里，不得写进正文当成正式规则。
只列真正影响研发实现或测试口径的问题，确认后回填正文并删除对应行。
单页面 PRD 去掉「涉及页面」列，用 | 序号 | 待确认事项 | 状态 |。 -->

| 序号 | 待确认事项 | 涉及页面 | 状态 |
| --- | --- | --- | --- |
| 1 | <待确认问题> | <页面名> | 待确认 |
