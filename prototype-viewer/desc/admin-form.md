# 新增推广方

## 页面目的

新增推广合作方，并维护资质、收款账户和分成配置。

## 交互说明

在 720px 右抽屉中填写基础信息、上传营业执照、录入收款账户；分成配置为非必填项，可选择分账方式并按拍摄点新增规则。取消返回订单列表，保存通过校验后给出成功反馈。

## 业务规则

推广方名称、联系人、11 位手机号、营业执照和收款账户必填。营业执照支持 JPG、JPEG、PNG、PDF，单文件不超过 2MB。分成比例为 0–100 的整数，同一拍摄点只能配置一条规则。

## 异常情况

必填项缺失、手机号格式错误、文件格式或大小不合规时阻止提交并就近提示；已填写内容保持不变。
